# JWT Configuration
JWT_SECRET=your-secure-random-secret-at-least-32-bytes-long-here-xxxxxxxxxxxx
JWT_EXPIRY_SECONDS=604800

# Optional: hardcoded whitelist wallets (comma-separated pubkeys)
# ALLOWED_WALLETS=pubkey1,pubkey2,pubkey3

# Environment
NODE_ENV=development
