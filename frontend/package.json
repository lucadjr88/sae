{
  "name": "sae-frontend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc && tsc-alias && node ../scripts/add-js-extensions.cjs ./dist && npm run copy-assets",
    "build:watch": "tsc --watch",
    "copy-assets": "cp -r src/index.html src/styles src/config src/assets dist/ 2>/dev/null || true"
  },
  "devDependencies": {
    "tsc-alias": "^1.8.16",
    "typescript": "^5.7.3"
  }
}
