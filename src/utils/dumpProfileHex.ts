// Utility per dump profilo in hex (stub, da integrare con fetch reale)
export async function dumpProfileHexUtil(profileId: string): Promise<{ outPath: string; length: number }> {
  // TODO: integrare fetch reale e scrittura file
  return { outPath: '', length: 0 };
}
