// Utility per refresh allowed wallets (stub, da integrare con fetch reale)
export async function refreshAllowedWalletsUtil(profileId: string): Promise<{ allowedWallets: string[] }> {
  // TODO: integrare fetch reale
  return { allowedWallets: [] };
}
